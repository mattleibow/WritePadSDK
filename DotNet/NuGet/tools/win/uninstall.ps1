param($installPath, $toolsPath, $package, $project)

    Write-Host "PhatWare.WritePad was removed, however the extension SDK 'Microsoft Visual C++ 2013 Runtime Package for Windows' was not as it may be a dependent reference on other things in your project.  If you do not need it, manually remove it."
